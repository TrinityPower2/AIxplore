import React, { useEffect, useRef, useState } from 'react';
import { View, StyleSheet, Text } from 'react-native';

const LoginPage = () => {

    return (
        <Text style={styles.text}>Page pour Médéric</Text>
    );
};

const styles = StyleSheet.create({
    text: {
        top: '50%',
        textAlign: 'center',
        color: '#384454'
    }
});

export default LoginPage;
